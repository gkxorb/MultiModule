
-- H2 DB 설치 후 데이터베이스 파일 생성.
-- (Window의 경우. 사용자의 Home 폴더에 test.mv.db 파일 생성됨.
jdbc:h2:~/test

-- 테스트 테이블 생성.
create table member
(
id bigint generated by default as identity,
name varchar(255),
primary key (id)
);